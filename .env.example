# Copy to .env and fill in. Do not commit .env.

# Optional: persistent storage for integrations, memory, conversations (survives restarts).
# Use an absolute path, e.g. D:\MasterAgentData or %APPDATA%\MasterAgentOS (Windows).
# APP_DATA_DIR=

# OpenAI (or set OPENAI_BASE_URL for local LLM)
# OPENAI_API_KEY=

# Google OAuth (Integrations)
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
# GOOGLE_REDIRECT_URI=http://localhost:6001/api/integrations/google/callback

# SendGrid
# SENDGRID_API_KEY=
# MAIL_FROM_EMAIL=

# Twilio
# TWILIO_ACCOUNT_SID=
# TWILIO_AUTH_TOKEN=
# TWILIO_PHONE_NUMBER=

# Redis (BullMQ)
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=

# Web automation (browser_* tools): use system Chrome instead of bundled Chromium
# BROWSER_CHANNEL=chrome
# Optional: run browser visible (default is headless)
# BROWSER_HEADED=true

# Connect to user's existing Chrome so agent can extract from already-open tabs (e.g. logged-in pages).
# Start Chrome with: chrome.exe --remote-debugging-port=9222
# CHROME_CDP_URL=http://localhost:9222

# Web search (web_search tool). Use one of:
# Google (same project as OAuth): enable Custom Search API, create Programmable Search Engine at programmablesearchengine.google.com
# GOOGLE_API_KEY=
# GOOGLE_CUSTOM_SEARCH_ENGINE_ID=
# Or Serper: https://serper.dev (2,500 free queries/month)
# SERPER_API_KEY=

# Ollama (when OPENAI_API_KEY is not set). Default: http://localhost:11434
# OLLAMA_HOST=http://localhost:11434
# OLLAMA_MODEL=llama3.2

# Running on two PCs: use local Ollama on each PC so they don't share one instance.
# On each PC leave OLLAMA_HOST unset or set to http://localhost:11434. Do not point
# both apps at the same Ollama (e.g. one PC's IP) or that Ollama may be overloaded.
