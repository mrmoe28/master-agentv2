# Google OAuth (Gmail, Calendar, Drive)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:3000/api/integrations/google/callback
GOOGLE_USER_ID=

# Twilio (SMS, Voice)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=

# SendGrid (Bulk email). To send from your domain (e.g. noreply@mastersagent.com):
# 1. In SendGrid: Settings → Sender Authentication → verify mastersagent.com (add their DNS records in Cloudflare).
# 2. Set the from address below. App can stay on localhost.
SENDGRID_API_KEY=
# MAIL_FROM_EMAIL=noreply@mastersagent.com
# MAIL_FROM_NAME=Master Agent

# Optional: encryption key for token storage (32-byte hex). If unset, tokens stored in plain form (dev only).
TOKEN_ENCRYPTION_KEY=

# Optional: absolute path for OAuth tokens file. If unset, uses <project>/data/oauth-tokens.json.
# Set this to a fixed path (e.g. in AppData or user home) so integrations stay connected after restart.
# OAUTH_TOKENS_FILE=

# Ollama (local LLM). Used when OPENAI_API_KEY is not set.
# Default: http://localhost:11434
OLLAMA_HOST=
# Optional: model name (e.g. llama3.2, mistral). Default: llama3.2
OLLAMA_MODEL=

# Desktop autonomy (in-app assistant: create folders, create files, open URL in browser).
# Root path for create_folder and create_file. If unset, uses the app's current working directory.
# DESKTOP_AGENT_WORKSPACE=
# Set to 1, true, or yes to allow creating files/folders on the user's Desktop (and use on_desktop in tools).
# DESKTOP_AGENT_ALLOW_DESKTOP=1
